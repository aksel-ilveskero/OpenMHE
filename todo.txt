# DONE
- Creating solver from state space
- Objective function building logic
- Process error (ProcessTerm)
- Measurement error term (MeasurementTerm)
- Input measurement error term (InputTrackingTerm)
- Building noise matrices from user input
- Running the solver (Python run_solver)
- Penalty functions
    - BasePenalty
    - L1 penalty
    - L2 penalty
    - Huber penalty
    - Deadzone penalty
- Input regulating term
    - First difference
    - Second difference
    - Random walk
- Arrival costs (Python + C EKF)
    - Steady-state / fixed P
    - EKF dynamic P (in-C incremental filter in run_c_solver)
- C sliding-window MHE driver (run_c_solver / run_loop.c)
    - Pointer-shifted yrefs, stack buffers sized from codegen
    - In-C incremental EKF arrival (x_bar, W0 per window in run_loop.c)
    - Bulk warm-start: ocp_nlp_get_all + memmove + set_all
    - Input pins (lbu/ubu) only when values change
    - Rebuild when acados header dimensions change (no stale NX)
    - post_steps hook (e.g. PI observer in ot_4_disk)
- filter_arrival.c: EKF/UKF + arrival-weight inversion + Cholesky (linked in run lib)
- LTI + LINEAR_LS fast path (lti_fast.c)
    - Window 0: full SQP-RTI + condense_qp_lhs
    - Windows 1+: vector-only QP update + precondensed_lhs solve (~25% solve time on ot_22)
    - Requires SQP_RTI; warn and fall back to full solve otherwise
    - Dynamic EKF arrival: stage-0 Hessian refresh, LHS invalidated per window
    - KnownInput pins via update_qp_matrices (not vectors-only)
    - tests/test_lti_fast.py regression suite
- Codegen export: openmhe_mhe_extra.{h,c} (A, B, Vx, Vu, W) for tooling / future RHS assembly
- QP defaults tuned for sliding windows
    - hpipm_mode=SPEED_ABS
    - qp_solver_cond_N=N_horizon (avoid extra condensing overhead)
- Profiling: OPENMHE_PROFILE=1 (setup/solve/shift + Acados lin/qp/reg/glob per-window)
- ot_22_disk: load_feather(..., N=) measurement subsampling
- Fast-path + C driver docs (openmhe/c_solver/README.md, root README)

# TODO
- Input regulating term
    - Custom input format

- Secondary PI loop for unknown inputs
    - PILoadObserver + post_steps in ot_4_disk (done); generalize / document in package API?

- Hard constraints for physically impossible solutions
- Soft constraints for infeasable solutions
- Slew rates for states as well

- Arrival costs
    - UKF in run_c_solver (use run_solver for UKF until in-C UKF is fixed at scale)

- Real-time performance (target: solve + overhead < dt, ~1 ms on ot_22)
    - Baseline (N=3 subsample): ~1.8 ms/window full RTI, ~1.34 ms/window fast path
    - Phase 2 fast path: standalone QP RHS from openmhe_mhe_extra (skip Acados lin submodules)
    - Smaller QP if acceptable: fewer augmented regulator states, horizon tuning
    - Further qp_solver / condensing experiments if needed after Phase 2

- Parameter estimation
- Support for multi-rate sensors

- C driver / test polish (low priority)
    - In-place warm-start via blasfeo_dveccp on nlp_out->ux (uniform nx/nu assumed)
    - Test isolation: test_c_runner_parity fails when run after test_lti_fast in same process (Acados shared-lib dimension pollution)

- Numerics (optional)
    - Per-state diagonal scaling for stiff OpenTorsion minimal realizations (float32 ruled out for 22-disk model)
